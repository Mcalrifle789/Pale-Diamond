{
  "name": "pale-diamond",
  "version": "1.0.0",
  "private": true,
  "description": "Pale Diamond — a web-hosted AI agent for tasks and automation.",
  "scripts": {
    "clean": "node scripts/build.mjs --clean",
    "build": "tsc -p tsconfig.json && node scripts/build.mjs",
    "dev": "node scripts/build.mjs --serve"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
